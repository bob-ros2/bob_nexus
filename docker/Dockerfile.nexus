ARG BASE_IMAGE=av
FROM bob-nexus:${BASE_IMAGE}

# Isolated Nexus Environment Blueprint
LABEL maintainer="Bob Nexus Architect"
LABEL version="3.7-isolated"

# Ensure the environment is ready for isolated execution
WORKDIR /app
